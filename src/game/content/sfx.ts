import type { SfxCatalog } from "../schema/pack";

export const sfxCatalog: SfxCatalog = {
  basePath: "/game/sfx/",
  ids: ["stat-up", "stat-down", "choice-select", "stage-clear", "ending-reveal"],
};
